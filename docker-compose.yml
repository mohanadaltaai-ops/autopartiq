services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: autopartiq
      POSTGRES_USER: autopartiq
      POSTGRES_PASSWORD: autopartiq
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
